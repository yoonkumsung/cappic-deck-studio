{
  "name": "cappic-deck-studio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "live-server src --port=3000 --no-browser --ignore=slides,data,versions",
    "open": "live-server src --port=3000 --ignore=slides,data,versions",
    "export": "node export/server.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5",
    "pdfjs-dist": "^3.11.174",
    "playwright": "^1.58.2",
    "sharp": "^0.34.5"
  }
}
